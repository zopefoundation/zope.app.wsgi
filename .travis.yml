# manual (marker for update script)
language: python
python:
    - 2.7
    - pypy-5.4.1
    - 3.3
    - 3.4
    - 3.5
install:
    - pip install -U pip
    - pip install -U -e .[test]
script:
    - zope-testrunner --test-path=src
notifications:
    email: false
cache: pip
